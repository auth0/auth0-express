<h1>Response from the API</h1>
<p>The web app obtained an access token for <code><%= audience %></code> and called the resource server with it.</p>
<pre><%= JSON.stringify(apiResponse, null, 2) %></pre>
