AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_SESSION_SECRET=
APP_BASE_URL=http://localhost:3000

# Secret used to sign this app's own session cookie (the app owns the session
# in Enterprise Connect mode — Auth0 writes none).
APP_SESSION_SECRET=
